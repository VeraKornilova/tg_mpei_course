//https://leetcode.com/problems/fibonacci-number/
class Solution {
public:
    int fib(int N) {
        int F;
        if (N==0) F=0;
            else if(N==1) F=1;
                else F=fib(N-1)+fib(N-2);
      return F ; 
    }
};
